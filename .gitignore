# WorkBuddy 内部目录：记忆 / 技能 / 临时发布基线，不入库
.workbuddy/

# 系统文件
.DS_Store

# 依赖（云函数等运行时安装，不入库）
node_modules/

# 误生成的文件（终端宽度输出残留）
--width

# 素材与参考图（index.html 已内联所需资源，运行时无需这些文件）
prototype/assets/
design-concepts/

# 含真实密钥（CloudBase accessKey），禁止入库；用户按 README 自行粘贴
pet-ai-demo/config.js

# 微信虚拟支付密钥（AppSecret/OfferID/AppKey），禁止入库；用 config.example.js 作模板
# ⚠️ 部署注意：CloudBase CLI 打包会排除 .gitignore 文件，故部署 payService 前需临时移除此行，
#   部署后务必恢复，避免密钥入库。长期建议改用云函数「环境变量」（不进代码/不进 git）。
cloud/payService/config.js
cloud/payNotify/token.json
