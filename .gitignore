# WorkBuddy 内部目录：记忆 / 技能 / 临时发布基线，不入库
.workbuddy/

# 系统文件
.DS_Store

# 依赖（云函数等运行时安装，不入库）
node_modules/

# 误生成的文件（终端宽度输出残留）
--width

# 素材与参考图（index.html 已内联所需资源，运行时无需这些文件）
prototype/assets/
design-concepts/

# 含真实密钥（CloudBase accessKey），禁止入库；用户按 README 自行粘贴
pet-ai-demo/config.js

# 微信虚拟支付密钥：已迁移到云函数「环境变量」（PAY_APP_SECRET / PAY_OFFER_ID / PAY_APP_KEY），
# 不进代码/不进 git，重部署也不丢（见 cloud/payService/config.js 顶部说明）。
# config.js 现在只含非敏感的 appid/productMap + 读环境变量，可正常入库与部署。
# payNotify 的 MP_TOKEN 同理走环境变量（见 cloud/payNotify/index.js）。
cloud/payNotify/token.json
