# 毛茸清单 · 部署与上线 Runbook

> 适用阶段：P4 代码已完成，准备做 P5 提审上线。
> 本文件把「云函数部署」和「微信个人虚拟支付 3 步配置」「P5 提审清单」一次性整理清楚。
> 环境：`cloud1-d4gck1kjyb8ca2456`，小程序 AppID：`wx538ee0892e9e7b2f`。

---

## 0. 前置准备（本机一次性）

```bash
# 1) 安装 CloudBase CLI（提供 tcb 命令）
npm i -g @cloudbase/cli

# 2) 登录（浏览器扫码 / 设备码，登录态会缓存）
tcb login

# 3) 确认可连到环境
tcb fn list
```

> 本机已装 `@cloudbase/cli@3.8.1`，二进制在
> `/Users/luomingxin/.npm-global/lib/node_modules/@cloudbase/cli/bin/tcb`；
> 若 `tcb` 不在 PATH，脚本会自动回退到该路径。

---

## A. 云函数部署

### A.1 一键脚本（推荐）

```bash
bash scripts/deploy-cloud-functions.sh
```

脚本会依次部署 7 个函数，每个都：清理本地 `node_modules` → 云端自动安装依赖（`--install-dependency true`）→ 上传。
配置（超时/内存/运行时）取自根目录 `cloudbaserc.json`：

| 函数 | 类型 | 超时 | 说明 |
|---|---|---|---|
| auth | Event | 20s | 匿名登录 + 落库 users/user_pets |
| taskService | Event | 20s | 待办 CRUD |
| sessionService | Event | 20s | 陪伴会话 + 足迹 |
| petService | Event | 20s | 宠物目录 / 解锁 / 切换 |
| payService | Event | 20s | 虚拟支付双签名下单 + 兜底发货 |
| petChat | Event | **60s** | 服务端 AI 对话（AI 生成需更久） |
| payNotify | **HTTP** | 20s | 虚拟支付发货推送接收（`--httpFn --path /payNotify`） |

### A.2 手动逐项（脚本不便时）

```bash
# Event 函数示例
tcb fn deploy auth --envId cloud1-d4gck1kjyb8ca2456 --runtime Nodejs20.19 --install-dependency true --force
# HTTP 函数（payNotify）
tcb fn deploy payNotify --envId cloud1-d4gck1kjyb8ca2456 --httpFn --path /payNotify --runtime Nodejs20.19 --install-dependency true --force
```

### A.3 部署后验证

```bash
tcb fn list                 # 应看到 7 个函数
tcb fn invoke auth          # 触发一次，确认不报错（返回 {ok:true, openid:...}）
tcb fn log petChat          # 看 AI 对话云函数日志
```

**两项必须人工确认：**

1. **petChat 函数超时改为 60 秒（必做！CLI 部署未生效）**
   控制台 → 云函数 → petChat → 函数配置 → 「基础配置」右侧「编辑」→ 函数超时改为 **60 秒** → 保存。
   默认 3 秒，AI 生成需要几十秒，不改必超时。⚠️ 注意：以后每次用 CLI 重新部署后要回来看一眼是否被重置回 3s。

2. **petChat 预置并发 = 1（消除冷启动，可选）**
   新版控制台没有「实例配置/最小实例数」，对应功能叫 **预置并发**：
   - ① 先发布版本：petChat 详情页顶部「版管理」→ 基于当前代码新建/发布一个版本
     （预置并发**不支持 $LATEST 版本**，只能配在已发布的快照版本上）。
   - ② 回到云函数**列表页** → petChat 行「操作」列 → **「预置管理」** → 新增预置并发 →
     选刚发布的版本 → 并发实例数 **1** → 确认。
   - 注意：预置实例会产生少量常驻费用（256MB 很低，个人量级每月约几毛~几元）；
     若想省钱可跳过，代价是首次打开 AI 问候慢 3–8s（之后正常）。
   - ⚠️ 每次 CLI 重新部署会更新 $LATEST，**预置并发挂在旧快照版本上**——重新部署后
     需重新发布新版本并更新预置并发，否则常驻的还是旧代码。

3. **payNotify 开启 HTTP 访问**
   控制台 → 云函数 → payNotify → 确认 HTTP 访问已开启，复制其 **HTTP 路径**
   （形如 `https://<envId>.api.tcloudbasegateway.com/payNotify`），B4 步要用。

---

## B. 微信个人虚拟支付 · 3 步配置（控制台，必须本人操作）

> 这些步骤涉及账号权限与密钥，**AI 无法代点**。代码侧（payService / payNotify / pets 页）已全部就绪，
> 你只需要在微信 MP 后台把配置补齐即可真机跑通。

### B1. 开通虚拟支付（✅ 你已完成）

- 入口：微信公众平台（MP）→ **虚拟支付**
- 前提（个人主体）：
  - 小程序主体为**个人**；
  - 服务类目含「工具」类（或符合个人虚拟支付开放类目）；
  - 已完成**微信认证 / 备案**。
- 月支付限额：10 万元（个人主体上限）。
- 开通后，在「基本配置」拿 **OfferID** 与 **现网 AppKey**。

### B2. 创建 12 个道具（每个星座宠物一个，¥6）

MP → 虚拟支付 → **道具管理** → 新建道具，逐个创建并**发布**：

| 宠物 key | 宠物名 | 建议 productId | 价格 |
|---|---|---|---|
| aries | 白羊 | `zodiac_aries` | ¥6（600 分） |
| taurus | 金牛 | `zodiac_taurus` | ¥6 |
| gemini | 双子 | `zodiac_gemini` | ¥6 |
| cancer | 巨蟹 | `zodiac_cancer` | ¥6 |
| leo | 狮子 | `zodiac_leo` | ¥6 |
| virgo | 处女 | `zodiac_virgo` | ¥6 |
| libra | 天秤 | `zodiac_libra` | ¥6 |
| scorpio | 天蝎 | `zodiac_scorpio` | ¥6 |
| sagittarius | 射手 | `zodiac_sagittarius` | ¥6 |
| capricorn | 摩羯 | `zodiac_capricorn` | ¥6 |
| aquarius | 水瓶 | `zodiac_aquarius` | ¥6 |
| pisces | 双鱼 | `zodiac_pisces` | ¥6 |

> 价格务必与 `cloud/petService/pets.js` 中每个宠物的 `price`（单位：元）一致（文案写 ¥6）。
> `productId` 命名任意，关键是和下一步 config.js 里的 `productMap` 对上。

### B3. 把 OfferID / AppKey / AppSecret 填进 config.js

> ⚠️ 安全：`cloud/payService/config.js` 已被 `.gitignore` 忽略，**不会被提交到 GitHub**（避免 AppSecret 泄露）。
> 仓库里只保留 `config.example.js` 模板。首次配置请先：
> ```bash
> cp cloud/payService/config.example.js cloud/payService/config.js
> ```
> 然后在本地编辑器打开 `config.js` 填入真实值（不要提交它）。

编辑 `cloud/payService/config.js`，把占位符替换为真实值：

```js
module.exports = {
  appid: 'wx538ee0892e9e7b2f',
  appSecret: '这里填 MP→开发设置→AppSecret',        // 仅服务端用，勿泄露到前端
  offerId: '这里填 虚拟支付→基本配置 的 OfferID',
  appKey: '这里填 虚拟支付→基本配置 的 现网 AppKey',  // 用现网，不用沙箱
  productMap: {
    aries: 'zodiac_aries',
    taurus: 'zodiac_taurus',
    // ……其余 10 个按 B2 表填真实 productId
  }
};
```

填完后**重新部署 payService**（`bash scripts/deploy-cloud-functions.sh` 会覆盖，或直接 `tcb fn deploy payService`）。

### B4. MP 后台【消息推送】指向 payNotify

- 入口：MP → 开发管理 → **消息推送**（或虚拟支付对应回调配置）。
- 把「虚拟支付发货通知（xpay_goods_deliver_notify）」的回调地址，指向 **A.3 拿到的 payNotify HTTP 路径**。
- payNotify 收到推送后会按 `outTradeNo` 幂等写入 `user_pets`（发货），返回 `<xml><ErrCode>0</ErrCode></xml>`。
- 若推送偶发丢失，`payService` 的 `confirmPay` / `query` 作为兜底发货。

> 开发期若只想验证解锁流程，可临时把 `pages/pets/pets.js` 顶部的 `DEV_DEMO` 置为 `true`，
> 走演示闭环（不接真支付即可解锁），真机上线前改回 `false`。

---

## C. P5 提审上线 Checklist

### C.1 真机预览与自测（微信开发者工具）
- [ ] 导入项目（已含 `project.config.json`，AppID 正确）。
- [ ] 编译 → **预览** → 手机扫码真机打开。
- [ ] 登录：首页能拿到宠物开场问候（说明 auth + 匿名登录通了）。
- [ ] 待办：新增 / 勾选完成 / 删除，完成时有宠物反馈气泡 + 写足迹。
- [ ] 专注 / 冥想：倒计时跑完写足迹。
- [ ] 宠物页：橘小满免费；星座宠物在 `DEV_DEMO=false` 时走 `wx.requestVirtualPayment`，支付成功自动解锁并切换。
- [ ] 对话页：点首页宠物卡片进入，「让宠物说一句」返回宠物语气的话（petChat 接通）。
- [ ] 我的页：羁绊数字、足迹日历、回忆册正常。

### C.2 隐私与合规
- [ ] 小程序后台已配置**隐私保护指引**（收集 openid 需声明）。
- [ ] 类目 / 资质符合要求（个人 + 工具类目）。
- [ ] 如接了真实支付，确认虚拟支付类目已开通。

### C.3 提交审核
- MP → 版本管理 → 上传（开发者工具「上传」）→ 填版本号与备注 → **提交审核**。
- 审核关注点：虚拟支付需走官方 `wx.requestVirtualPayment`，不得引导外部支付；「用户不能主动发消息、宠物只读待办做提醒」的产品形态要在功能说明里讲清。

---

## D. 文件清单与改动点

| 文件 | 作用 | 是否含密钥 |
|---|---|---|
| `cloudbaserc.json` | 部署配置（envId + 7 函数超时/内存/运行时） | 否 |
| `scripts/deploy-cloud-functions.sh` | 批量部署脚本 | 否 |
| `cloud/payService/config.example.js` | 虚拟支付配置模板（已入库，不含密钥） | 否 |
| `cloud/payService/config.js` | 真实密钥（**已被 .gitignore 忽略，不入库**） | **本地填，勿提交** |
| `cloud/petChat/config.json` | petChat 运行时/超时（已改 Nodejs20.19 / 60s） | 否 |
| `cloud/payNotify/index.js` | 发货推送接收（HTTP） | 否 |
| `cloud/payService/index.js` | 双签名下单 + 兜底发货 | 否 |
| `pages/pets/pets.js` | `DEV_DEMO` 开关 + `wx.requestVirtualPayment` 接通 | 否 |

> 密钥（`appSecret` / `offerId` / `appKey`）只在服务端 `config.js` 使用，绝不下发前端；
> `config.js` 已被 `.gitignore` 忽略，不会进仓库；仓库里只有 `config.example.js` 模板，安全可提交。
