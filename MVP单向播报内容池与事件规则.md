# 陪伴式任务清单 · MVP 单向播报内容池与事件规则

> 版本 v0.2 · 2026-09-03
> 配套：《产品想法-陪伴式任务清单小程序》（v113 去金币化 / v115 运行锁定线）
> 用途：给前端 + 云函数提供播报文案池、命中条件与数据规则，可直接照着实现
> 范围：MVP 纯单向播报。已拍板——不做「点嗯」微回应、无输入框、无多轮对话。
> v0.2 修订：里程碑 M1~M3 统一为「连续完成天数」一条线（原 M3「累计 20 件」与 M1/M2 维度不一致，移出 MVP，见 §7）。

---

## 1. 播报模型：两条铁律

**铁律 1 · 单向在场**
宠物只会「说」，不会「听」。所有播报都发生在用户**打开小程序或操作任务的当场**——微信不允许小程序在用户没打开时随意弹消息（订阅消息需逐次授权，不进 MVP）。因此本清单没有任何「后台推送」类触发。

**铁律 2 · 只记得真实发生的事**
每条「记得你」都必须对应一条真实的行为数据；数据缺失时**不脑补、不编造**，降级到更浅一档的文案。MVP 全部文案 = **预写模板 + 变量填充**，不引入 AI 实时造句（省钱、可控、不跑偏）；AI 只在后续的「每周画像」阶段离线使用。

---

## 2. 数据依赖清单（模板变量的唯一合法来源）

| 数据 | 字段/判定 | 原型现状 | 缺口 |
|---|---|---|---|
| 任务名 | `task.title` | ✅ 已有 | — |
| 完成时刻 | `task.done_at`（勾掉时写时间戳） | ⚠️ 只有 done 状态，无时间 | 需补 |
| 昨日完成列表 | 由 `done_at` 按自然日聚合 | 可算 | — |
| 连续完成天数 | `streak`：每天至少完成 1 件 = 算 1 天，断档归零 | 可算 | 需实现聚合 |
| 逾期任务 | `created_at` 距今 > 48h 且仍 pending（近似逾期） | 可算 | 判定规则；将来有「计划时间」字段后换精确判定 |
| 播报去重日志 | `broadcast_log`：{日期, 槽位, 模板ID, 关联任务ID} | ❌ 无 | 需补 |
| 回忆册 | 现有 sessions / 任务记录 | ✅ 已有 | 里程碑条目需支持追加 |

> 原型任务目前**没有「计划时间」字段**，所以 MVP 用「创建超过 48h 仍未完成」近似「拖延」，不阻塞上线；将来给任务加可选计划时间后，把判定换成 `planned_at < now` 即可，文案不变。

---

## 3. 四类播报

### 3.1 每日开场（原「登录」）— 每天最多 1 条

**触发**：进入首页。
**去重**：`broadcast_log` 按 (用户, 自然日, 槽位=open) 记一条，同一天重复进入不再播。
**选句规则**：只挑下面优先级最高的 1 档说，**不叠着说**。

| 优先级 | 状态 | 说的事 |
|---|---|---|
| P1 | 有逾期任务（>48h 未完成） | 轻戳一句逾期 |
| P2 | streak 连续 ≥2 天 | 确认「你在坚持」 |
| P3 | 昨天有完成记录 | 复述昨天完成的事 |
| P4 | 以上都没有（新用户/空白日） | 兜底陪伴语 |

**P1 · 逾期轻戳**（缺逾期任务名时降级到 P4，不硬说）

1. `{任务名}` 还在清单里躺着。今天做不做都行——反正我在这儿陪着你。
2. `{任务名}` 你放了两天多了，我数着呢。不催，就是让你知道我还记得。
3. 今天的清单第一行，还是那件熟悉的 `{任务名}`。要不要先把它划掉？

**P2 · 坚持确认**（需要 streak ≥ 2；无 streak 数据降级到 P3/P4，**不得虚构天数**）

1. 你已经连着 `{N}` 天把当天的事做完了。橘小满觉得你挺稳的。
2. 连续 `{N}` 天，一天没落。你大概没注意，我替你记着。
3. 昨天的你也好好收尾了。今天的你，看着也不差。

**P3 · 复述昨天**（需要昨日完成列表；空则降级 P4）

1. 昨天那件 `{任务名}`，我写进回忆册了。
2. 昨天一共划掉了 `{n}` 件事。我在旁边看得清清楚楚。
3. 回忆册里又多了一笔昨天的：`{任务名}`。

**P4 · 兜底陪伴**（唯一允许「无数据也自然」的档）

1. 你来了。清单今天想从哪件开始？
2. 想做就做，不想做在这儿歇会儿也行，我又不赶你。
3. 你好呀，我叫橘小满。你去做你的事，我在这儿等你。

---

### 3.2 完成一条待办 — 当场 1 条

**触发**：用户在清单勾掉一条任务（打勾后即播）。
**去重**：同一条任务只播 1 次（`broadcast_log` 记 taskId）；撤销完成再完成视为同一条，不重复播。
**模板**（普通完成，3 条轮换；若当天开场已复述过「昨天完成」，此处只准用 1 号纯夸，避免同一事实说两遍）：

1. 划掉了。清单上少了一行，心里是不是也松了一点。
2. 又搞定一件。这笔记进今天的回忆册了。 ← 对应现有「已收入回忆册」气泡语义
3. 你完成的事，我一件件都记得。

**迟到完成变体**（当这条任务已逾期 >48h 才被完成）：优先说这句，不再叠加普通模板——

- 拖了这么久，最后不还是做完了。这样也算数，橘小满照样记一笔。

---

### 3.3 拖延 — 产品决定：只在两处出现，不单开「唠叨嘴」

连续提醒逾期会让清单变成压力源，违背陪伴定位。因此 MVP 的「拖延」只出现在两个真实瞬间：

1. **每日开场 P1**（3.1）：打开时若有逾期，轻戳一次，当天不再提。
2. **迟到完成**（3.2 变体）：逾期任务终于被划掉时，给「迟到也作数」的鼓励，不翻旧账。

其余时间不对逾期任务单独弹窗。逾期任务在清单里的视觉状态（置灰/标记）由清单页负责，不归播报管。

---

### 3.4 里程碑 — 当场庆祝 + 写入回忆册

**触发与档位判定**（完成打勾的瞬间检查，命中才播）：

| 档位 | 判定 | 含义 |
|---|---|---|
| M1 | streak 达到 3 天 | 第一个小里程碑 |
| M2 | streak 达到 7 天 | 连续一周 |
| M3 | streak 达到 14 天 | 连续两周 |

**维度统一**：M1~M3 全部走「连续完成天数」一条线，同维度递增，语义一致。曾考虑的「累计完成 N 件」属于另一条成就线（服务高频完成者），与「坚持」不是一回事，MVP 不混入 M 序列——已移出，见 §7 扩展。

**命中后动作**：气泡庆祝 1 条 + 往回忆册追加一条「里程碑」事件（回忆册需支持该事件类型，见 §5）。
**同档只庆祝一次**（按档位去重，不会每周复读；将来可再加连续 30 天档位）。

1. M1：连续 3 天，一天没落。第一个小里程碑，橘小满给你记进回忆册了。
2. M2：一整周，每天都有收尾。你大概没发现，但我一直数着。
3. M3：整整两周，一天没断。这份坚持，橘小满替你收进回忆册了。

---

## 4. 去重与优先级规则（实现时照抄）

1. **开场槽**：每天 1 条；当天已发则跳过（含 P1~P4 任一档）。
2. **反应槽**：每条任务 1 条；里程碑按档位 1 次。
3. **同一事实不跨槽重复**：开场 P2/P3 说过「坚持/昨天」，当天完成反应只许用完成池第 1 条（纯夸）。
4. **模板轮换**：同一槽位的模板 7 天内不重复（池子够大时自然轮换，记 `template_id`）。
5. **缺数据降级**：任何模板缺必需变量 → 降到下一档/下一句，绝不填假数字。

---

## 5. 落地缺口与建议开发顺序

当前原型要实现本清单，按序补：

1. `task.done_at`：打勾时写时间戳（撤销完成时清空）。
2. 完成反应：把现有完成气泡的文案源换成 §3.2 内容池 + `broadcast_log` 去重。
3. 每日开场：首页进入时按 P1→P4 判档，用 `petBubble` 在橘小满旁播 1 条（气泡组件已存在，无需新 UI）。
4. 里程碑：streak 聚合 → 命中档位播 §3.4 并写回忆册「里程碑」事件。
5. 逾期：48h 近似判定接入 P1 与迟到完成变体；将来任务加「计划时间」后替换判定。

> 回忆册目前存的是 sessions/任务记录；里程碑是「事件」类型，需要在回忆册读取端加一个类型分支来展示。

---

## 6. MVP 明确不做（防范围蔓延）

- ❌ 「点嗯」微回应（已拍板暂缓，架构上保留入口）
- ❌ 用户自由文本输入、豆包式多轮对话
- ❌ 后台订阅消息推送（微信逐次授权限制）
- ❌ 语音播报
- ❌ AI 实时造句（文案全部预写模板；AI 仅用于后续每周画像）

---

## 7. 后续扩展（本期不做，仅存档方向）

- **累计成就线**：累计完成 N 件（20 / 50 / 100）的庆祝与回忆册条目，需 `total_done` 聚合。它与「连续天数」是两条独立的成就感线：连续线奖励「稳定的坚持」，累计线奖励「高频的完成」。做的时候用单独命名（如「成就」），**不混进 M 系列**。
- **每周画像**：定时任务离线把一周行为生成摘要（走 AI），供后续个性化文案注入。
